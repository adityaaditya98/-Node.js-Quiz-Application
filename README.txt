Save the Github file to your local drive.

Open your windows and CMD (Terminal).

Go to the Project Directory by navigating. Navigate to the project directory using your terminal.

Install Dependencies: To install every library mentioned in the package.json file, open your terminal in the project directory and type npm install.

once every dependency has been installed.

Launch the Programs:

To launch the server, enter either nodemon logic.js or node logic.js.

